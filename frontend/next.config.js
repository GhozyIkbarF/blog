/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["zqdhiliedxreoaupcbyq.supabase.co",'localhost'],
  },
};

module.exports = nextConfig;
